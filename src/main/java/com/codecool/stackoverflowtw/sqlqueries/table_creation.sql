CREATE TABLE questions (
                           id INT PRIMARY KEY,
                           title VARCHAR(255) NOT NULL,
                           description TEXT,
                           created TIMESTAMP
);